int main(){
    int c = func();
    int d = x*y + log(x);
    int q = x*y + log(x);
    A[2] = B[c];
    A[3] = B[c];
    // CONDICIONAL -> RAMIFICACION EN EL CODIGO
    // SE ARRIESGA CON UNA RAMA DE EJECUCION
    if( c > 0 ){
        //
        //...
        //
    else{
        //
        //..
    }
    //...
    //
    //
    //
    // INSTRUCCION CONTEXTO A (Mas de aritmetica)
    // INSTRUCCION CONTEXTO B (Mas de I/O, o acceso a memoria)
    // INSTRUCCION CONTEXTO A (Mas de aritmetica)
    // INSTRUCCION CONTEXTO B (Mas de I/O, o acceso a memoria)
    // INSTRUCCION CONTEXTO A (Mas de aritmetica)
    // INSTRUCCION CONTEXTO B (Mas de I/O, o acceso a memoria)
    //
    //
    // INSTRUCCION CONTEXTO A (Mas de aritmetica)   [1 unidad de flotante]
    // INSTRUCCION CONTEXTO A (Mas de aritmetica)
    // INSTRUCCION CONTEXTO A (Mas de aritmetica)
    // INSTRUCCION CONTEXTO A (Mas de aritmetica)
    // INSTRUCCION CONTEXTO A (Mas de aritmetica)
    // INSTRUCCION CONTEXTO A (Mas de aritmetica)
    // INSTRUCCION CONTEXTO A (Mas de aritmetica)
    // INSTRUCCION CONTEXTO B (Mas de I/O, o acceso a memoria)
    // INSTRUCCION CONTEXTO B (Mas de I/O, o acceso a memoria)
    // INSTRUCCION CONTEXTO B (Mas de I/O, o acceso a memoria)
    // INSTRUCCION CONTEXTO B (Mas de I/O, o acceso a memoria)
    // INSTRUCCION CONTEXTO B (Mas de I/O, o acceso a memoria)
    // INSTRUCCION CONTEXTO B (Mas de I/O, o acceso a memoria)
    // INSTRUCCION CONTEXTO B (Mas de I/O, o acceso a memoria)
}
