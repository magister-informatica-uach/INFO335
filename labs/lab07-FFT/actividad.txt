En este lab usted trabajara con la transformada rapida de Fourier (FFT) en sus distintas versiones.

1) Paralelizar el algoritmo recursivo usando openMP.
2) Paralelizar el algoritmo iterativo usando CUDA.
3) Mostrar un grafico que compare el rendimiento de ambas soluciones y una reflexion sobre la diferencia de rendimiento
entre las version recursiva e iterativa.
